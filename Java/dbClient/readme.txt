Simple Java tool to connect to a database, access, create and destroy buckets and tuples stored in 
the database. Currently supported riak and a dummy which allows to execute the actions which it is 
used for testing and demonstrations when not Internet or a riak database is available.

See the tool's help for more information.

Original code created by Amadeo Asco (trifork).
